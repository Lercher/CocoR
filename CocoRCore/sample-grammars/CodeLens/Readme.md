## Workflow Model with CodeLens

A sample grammar that parses a DSL for a workflow data model.
With CodeLens extensions, i.e. small commands inside the source in
your CodeLens supporting IDE, like vscode.